{
  "name": "bitemitra",
  "private": true,
  "description": "BiteMitra food delivery platform — Customer, Restaurant and Rider mobile apps, Admin web, API, workers and shared business engines (JavaScript monorepo).",
  "type": "module",
  "packageManager": "pnpm@11.24.0",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "verify:schema": "node scripts/verify-schema.mjs",
    "check:docs": "node scripts/check-docs.mjs",
    "verify": "pnpm verify:schema && pnpm check:docs"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8"
  }
}
