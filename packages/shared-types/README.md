# @jamzo/shared-types

Enums/constants (order statuses, roles, app ids, error codes, header names) and JSDoc `@typedef`s shared by every app and service. JavaScript only — no `.d.ts`/`.ts`. Values mirror the Prisma enums; a test enforces they never drift. Phase 1.
