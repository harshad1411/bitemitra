# @bitemitra/api-client

fetch-based client for all frontends: version headers (`x-app-id`, `x-app-version`, `x-platform`), timeouts, retry with backoff for idempotent requests, automatic `Idempotency-Key` on mutations, access-token refresh, standard error parsing. Phase 1.
