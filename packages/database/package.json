{
  "name": "@bitemitra/database",
  "version": "0.0.0",
  "private": true,
  "description": "Prisma schema, migrations, client and seed data. Phase 0: schema design only.",
  "type": "module",
  "scripts": {
    "db:validate": "prisma validate",
    "db:format": "prisma format"
  },
  "devDependencies": {
    "prisma": "~6.19.3"
  }
}
