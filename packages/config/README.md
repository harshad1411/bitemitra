# @bitemitra/config

Environment-variable schemas (per service, fail-fast), the **settings registry** (key, schema, default, allowed scopes, admin section), hierarchical resolution (GLOBAL → … → PRODUCT), feature-flag evaluation, app registry, semver helpers. Design: [docs/CONFIGURATION.md](../../docs/CONFIGURATION.md). Phase 1.
